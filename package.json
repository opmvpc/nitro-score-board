{
  "name": "example-hello-world",
  "private": true,
  "scripts": {
    "dev": "concurrently \"nitro dev\" \"vite front\"",
    "build": "nitro build && cd front && npx vite build --outDir ../public --emptyOutDir"
  },
  "devDependencies": {
    "@vercel/kv": "^0.2.3",
    "concurrently": "^8.2.1",
    "nitropack": "latest",
    "vite": "^4.4.5"
  }
}
